{
  "name": "web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "validate:story": "node scripts/validate-story.mjs",
    "release:cloudflare": "powershell -ExecutionPolicy Bypass -File .\\scripts\\prepare-cloudflare-release.ps1"
  },
  "devDependencies": {
    "vite": "^8.0.4"
  }
}
